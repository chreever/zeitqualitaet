backend:
  name: git-gateway
  branch: main

media_folder: "zeitqualitaet/img" # Pfad zu deinen Bildern
public_folder: "/img"

collections:
  - name: "blog"
    label: "Blogbeiträge"
    folder: "zeitqualitaet/posts" # Hier speichert das CMS die neuen Texte
    create: true
    slug: "{{slug}}"
    fields:
      - {label: "Titel", name: "title", widget: "string"}
      - {label: "Datum", name: "date", widget: "datetime"}
      - {label: "Vorschaubild", name: "image", widget: "image"}
      - {label: "Kurzbeschreibung", name: "description", widget: "text"}
      - {label: "Inhalt", name: "body", widget: "markdown"}
